# Central ESPHome config for OpenQueue
esphome:
  name: openqueue
  friendly_name: OpenQueue
  name_add_mac_suffix: true
  on_boot:
    then:
    - light.turn_on:
        id: openqueue_rgb
        effect: rainbow

esp32:
  board: esp32-s3-devkitc-1
  # framework:
  # type: esp-idf

packages:
  led: !include conf.d/led.yaml
  web_server: !include conf.d/web_server.yaml
  mqtt: !include conf.d/mqtt.yaml
  button: !include conf.d/button.yaml
  wifi: !include conf.d/wifi.yaml
  printago: !include conf.d/printago.yaml

logger:
  # level: VERBOSE
  # logs:
  #   mqtt.component: VERBOSE
  #   mqtt.client: VERBOSE
  #   mqtt: VERBOSE

  # api: #can not have mqtt and api at the same time

ota:
  platform: esphome

# esp32_improv:
#   authorizer: none

# improv_serial:

# substitutions:
#   board_type: esp32dev
#   rgb_led_pin: '4'
#   # mac_suffix: 'XXXX'
